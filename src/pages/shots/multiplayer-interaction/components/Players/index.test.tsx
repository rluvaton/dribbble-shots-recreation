import Players from './';

describe('Players', () => {
  it('should be defined', () => {
    expect(Players).toBeDefined();
  });

  it.todo('should pass show=true and animate=false for index 0');
  it.todo('should pass show=true and animate true for index > 0 when changing the number of players');
});
